@model IEnumerable<string>
@if (Model.Count() == 0)
{
  <h4>No Categories available</h4>
}
else
{
  @foreach (var item in Model)
  {
    <a class="btn btn-outline-primary btn-block" asp-controller="Product" asp-action="GetByCategory" asp-route-category="@item">@item</a>
  }
}